<?php

namespace Database\Seeders;

use App\Models\User;
use Illuminate\Database\Seeder;

class DatabaseSeeder extends Seeder
{
    /**
     * Seed the application's database.
     *
     * @return void
     */
    public function run()
    {
//        \App\Models\User::factory(5)->create();
//        \App\Models\Post::factory(20)->create();

//        $userA = User::factory()->create(['name' => 'Drow Ranger' , 'email' => 'drow@drow.com' , 'password' => '12345678']);
//        $userB = User::factory()->create(['name' => 'Shodow Fiend' , 'email' => 'shadow@shadow.com' , 'password' => '12345678']);
//
//        $userA->followings()->attach(User::factory(5)->create()->pluck('id'));
//        $userA->followers()->attach(User::factory(5)->create()->pluck('id'));
//
//        $userB->followings()->attach(User::factory(5)->create()->pluck('id'));
//        $userB->followers()->attach(User::factory(5)->create()->pluck('id'));

//        \App\Models\Like::factory(50)->create();
    }
}
